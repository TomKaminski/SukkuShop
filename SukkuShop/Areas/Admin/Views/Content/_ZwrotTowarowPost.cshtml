@model SukkuShop.Areas.Admin.Models.RegulaminModel
@Ajax.ActionLink("Zakończ edycję (bez zapisu)", MVC.Admin.Content.ZwrotTowarowAjaxPost(), new AjaxOptions { UpdateTargetId = "regulamin-replace", HttpMethod = "POST", InsertionMode = InsertionMode.Replace })
@using (Html.BeginForm(MVC.Admin.Content.ZwrotTowarow(), FormMethod.Post, new { validateRequest = "false" }))
{
    @Html.TextArea("Text", Model.Text)
    <p><input type="submit" value="Zapisz" /></p>
}
